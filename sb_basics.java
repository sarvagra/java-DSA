public class sb_basics{
    public static void main(String[] args) {
        StringBuilder sb=new StringBuilder("Sarvagra"); // declaration
        System.out.println(sb);
        StringBuilder st=new StringBuilder();  // creating empty stringbuider
        StringBuilder str=new StringBuilder(10);  // creating empty stringbuilder , but with capacity 10 (default cap is 16)
        
    }
}